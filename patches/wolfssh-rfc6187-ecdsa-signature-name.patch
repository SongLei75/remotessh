diff --git a/src/internal.c b/src/internal.c
index b2153e3..e56b48a 100644
--- a/src/internal.c
+++ b/src/internal.c
@@ -20624,0 +20625 @@ static int PrepareUserAuthRequestEccCert(WOLFSSH* ssh, word32* payloadSz,
+    const char* sigName = NULL;
@@ -20715 +20716,25 @@ static int PrepareUserAuthRequestEccCert(WOLFSSH* ssh, word32* payloadSz,
-            if (sigSz >= 0) {
+            /* RFC 6187 section 3.4 uses the X.509 algorithm name for the
+             * public key, but the signature blob keeps the corresponding
+             * RFC 5656 ECDSA algorithm name. */
+            switch (keySig->sigId) {
+                #ifndef WOLFSSH_NO_ECDSA_SHA2_NISTP256
+                case ID_X509V3_ECDSA_SHA2_NISTP256:
+                    sigName = cannedKeyAlgoEcc256Names;
+                    break;
+                #endif
+                #ifndef WOLFSSH_NO_ECDSA_SHA2_NISTP384
+                case ID_X509V3_ECDSA_SHA2_NISTP384:
+                    sigName = cannedKeyAlgoEcc384Names;
+                    break;
+                #endif
+                #ifndef WOLFSSH_NO_ECDSA_SHA2_NISTP521
+                case ID_X509V3_ECDSA_SHA2_NISTP521:
+                    sigName = cannedKeyAlgoEcc521Names;
+                    break;
+                #endif
+                default:
+                    ret = WS_INVALID_ALGO_ID;
+                    break;
+            }
+
+            if (ret == WS_SUCCESS && sigSz >= 0) {
@@ -20718 +20743 @@ static int PrepareUserAuthRequestEccCert(WOLFSSH* ssh, word32* payloadSz,
-                        authData->sf.publicKey.publicKeyTypeSz;
+                        (word32)WSTRLEN(sigName);
@@ -20721 +20746 @@ static int PrepareUserAuthRequestEccCert(WOLFSSH* ssh, word32* payloadSz,
-            else
+            else if (ret == WS_SUCCESS)
@@ -20915 +20940 @@ static int BuildUserAuthRequestEccCert(WOLFSSH* ssh,
-                    names = cannedKeyAlgoX509Ecc256Names;
+                    names = cannedKeyAlgoEcc256Names;
@@ -20920 +20945 @@ static int BuildUserAuthRequestEccCert(WOLFSSH* ssh,
-                    names = cannedKeyAlgoX509Ecc384Names;
+                    names = cannedKeyAlgoEcc384Names;
@@ -20925 +20950 @@ static int BuildUserAuthRequestEccCert(WOLFSSH* ssh,
-                    names = cannedKeyAlgoX509Ecc521Names;
+                    names = cannedKeyAlgoEcc521Names;
